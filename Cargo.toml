[package]
name = "openvcs-plugin-template"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
openvcs-core = { path = "../Core", features = ["plugin-protocol"] }
